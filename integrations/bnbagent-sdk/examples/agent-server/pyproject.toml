[project]
name = "agent-server"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "bnbagent[server]",
    "fastapi>=0.104.0",
    "httpx>=0.25.0",
    "ddgs>=0.0.1",
    "uvicorn[standard]>=0.24.0",
    "python-dotenv>=1.0.0",
]

[tool.uv.sources]
bnbagent = { path = "../..", editable = true }
