[project]
name = "guardrail-lab"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pandas",
  "numpy",
  "matplotlib",
  "plotly",
  "duckdb",
  "pyarrow",
  "jupyter",
  "pydantic",
  "sqlalchemy",
  "rich"
]

